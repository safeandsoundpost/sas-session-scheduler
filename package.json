{
  "name": "sas-scheduler",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute scheduler-db --file=migrations/0001_init.sql",
    "db:migrate:local": "wrangler d1 execute scheduler-db --local --file=migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
